- name: Installing nginx
  hosts: web
  gather_facts: no
  become: yes
  vars:
   number: 11
  tasks:
  - name: Installing nginx
    ansible.builtin.debug:
     msg: "{{gathering_facts}}"
    when: number | int > 10 


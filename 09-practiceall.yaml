- name: To register a user if not exists.
  hosts: web
  tasks:
  - name: To display ansible facts
    become: yes
    ansible.builtin.debug: 
     msg: "Hi {{item}}, elcome to ansible study sessions"
    loop:
    - Amar
    - Akhil
    - Abhinav
    - vishwa
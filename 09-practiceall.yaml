- name: Installing nginx
  hosts: web
  gather_facts: no
  become: yes
  vars_files:
  - var.yaml
  tasks:
  - name: Installing nginx
    ansible.builtin.debug:
     msg: "The course  i am learning is {{course}}"


- name: Installing nginx
  hosts: web
  gather_facts: no
  become: yes
  vars:
   number: 1
  tasks:
  - name: Installing nginx
    ansible.builtin.debug:
     msg: "I am going to be printed on a condition"
    when: number | int > 10 


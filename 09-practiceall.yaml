- name: Installing nginx
  hosts: web
  gather_facts: no
  become: yes
  vars_prompt:
  - name: course
    prompt: Tell me the course name you are learning
    private: false
  tasks:
  - name: Installing nginx
    ansible.builtin.debug:
     msg: "The course  i am learning is {{course}}"


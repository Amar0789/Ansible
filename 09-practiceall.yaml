- name: Installing nginx
  hosts: web
  gather_facts: no
  become: yes
  vars:
   number: 11
  tasks:
  - name: Installing nginx
    ansible.builtin.debug:
     msg: "I am going to be printed on a condition"
    when: "{{number | int > 10 }}"

